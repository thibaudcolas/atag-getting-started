{
  "name": "atag-getting-started",
  "version": "0.1.0",
  "license": "ISC",
  "author": "",
  "scripts": {
    "start": "open index.html && tailwindcss --watch --input styles.css --output output.css",
    "build": "tailwindcss --minify --input styles.css --output output.css"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "prettier": "^3.4.2",
    "tailwindcss": "^4.0.0"
  }
}
